# Copie para .env na VM e preencha os valores reais.
# NUNCA commite o .env com credenciais reais.

# ---- Oracle Database ----
# Senha usada para o Oracle SYS/SYSTEM E para o schema CPTM
# Use apenas letras e numeros para evitar problemas no SQL de inicializacao
ORACLE_PASSWORD=TrocaMeSenhaForte2024

# ---- JWT ----
# Gere com: openssl rand -hex 32
JWT_SECRET_KEY=cole_aqui_64_chars_hex_gerados_pelo_openssl_rand_hex_32

# ---- CORS ----
# URL exata do seu app no Vercel (sem barra no final)
FRONTEND_URL=https://cptm-app.vercel.app
